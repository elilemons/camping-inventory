import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.elilemons.campinginventory',
  appName: 'Camping Inventory',
  webDir: 'out',
}

export default config
